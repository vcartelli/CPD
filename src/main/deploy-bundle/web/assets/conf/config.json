{
  "server": {
    "origin": "${cpd.server.scheme}://${cpd.server.hostname}:${cpd.server.port}",
    "auth": {
      "base": "/auth",
      "login": "${cpd.server.auth.login}",
      "logout": "${cpd.server.auth.logout}",
      "getProfile": "${cpd.server.auth.getProfile}"
    },
    "api": {
      "base": "${cpd.server.api.base}",
      "getDiagramSummaryList": "${cpd.server.api.getDiagramSummaryList}",
      "getDiagram": "${cpd.server.api.getDiagram}",
      "getDiagramElements": "${cpd.server.api.getDiagramElements}",
      "getSemantic": "${cpd.server.api.getSemantic}",
      "putSemantic": "${cpd.server.api.putSemantic}"
    }
  },
  "qae": {
    "origin": "${cpd.qae.origin}",
    "api": {
      "base": "${cpd.qae.api.base}",
      "getQuestionCount": "${cpd.qae.api.getQuestionCount}"
    },
    "link": {
      "newQuestion": "${cpd.qae.link.newQuestion}",
      "relatedQuestions": "${cpd.qae.link.relatedQuestions}"
    }
  }
}
